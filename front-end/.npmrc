; Required when npm fails with SELF_SIGNED_CERT_IN_CHAIN (corporate TLS inspection).
; Remove this line if you are on a normal network and prefer strict certificate checks.
strict-ssl=false
fund=false
audit=false
